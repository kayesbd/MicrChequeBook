using System.Web.Http;

//using log4net;
//using log4net.Config;


namespace GITS.Web.Controllers
{


    public class BaseAPIController : ApiController
    {
      
       
        //protected void SetAuthInfo(int Id, string Email, string FullName, string Role, int PersonId, int OrganizationId
        //    , string exchangeUID, string exchangePWD, string exchangeDomain,int? fileManagerPermission)
        //{
        //    AuthUtil.SetAuthInfo(Id, Email, FullName, Role, PersonId, OrganizationId, exchangeUID, exchangePWD, 
        //        exchangeDomain, fileManagerPermission);
        //}
    }
   //[JulyExceptionHandler]
   //public class BaseODataAPIController : ODataController
   //{


   //    protected void SetAuthInfo(Guid Id, string Email, string FullName, string Role, Guid PersonId, Guid OrganizationId
   //        , string exchangeUID, string exchangePWD, string exchangeDomain)
   //    {
   //        AuthUtil.SetAuthInfo(Id, Email, FullName, Role, PersonId, OrganizationId, exchangeUID, exchangePWD, exchangeDomain);
   //    }
   //}
}
